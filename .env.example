# ============================================================
# DeFiZoo Agent Orchestrator — Environment Variables
# ============================================================
# Copy to .env and fill in your values:
#   cp .env.example .env
#
# Docs: https://docs.openclaw.ai/help/environment
# NEVER commit .env to version control.
# ============================================================

# --- LLM Provider API Keys (at least one required) ---

# Anthropic (recommended) — https://console.anthropic.com/
ANTHROPIC_API_KEY=

# OpenAI — https://platform.openai.com/api-keys
OPENAI_API_KEY=

# OpenRouter (multi-provider) — https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- Discord Bot (required) ---
# Create at https://discord.com/developers/applications
# Enable intents: Message Content + Server Members
# Bot permissions: View Channels, Send Messages, Read Message History,
#                  Embed Links, Attach Files, Add Reactions
# Also enable: applications.commands (for slash commands)
DISCORD_BOT_TOKEN=

# --- GitHub ---
# https://github.com/settings/tokens
# Scopes needed: repo, read:org, delete_repo (for full repo management)
GITHUB_TOKEN=

# GitHub user that owns the brain repos (used in AGENTS.md instructions)
GITHUB_ORG=DeFiZooKeeper

# --- OpenClaw Gateway ---
# Auth token for the Control UI and CLI health checks.
# Generate: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=

# Gateway bind mode: lan (container default) or local
OPENCLAW_GATEWAY_BIND=lan

# Exposed port on host (default: 18789)
OPENCLAW_PORT=18789

# Timezone
TZ=UTC

# --- Discord IDs (for multi-agent routing) ---
# Enable Developer Mode in Discord (User Settings > Advanced)
# Right-click to copy IDs
#
# These are used in state/openclaw.json — replace the REPLACE_* placeholders.
# DISCORD_GUILD_ID=
# DISCORD_FRONTEND_CHANNEL_ID=
# DISCORD_BACKEND_CHANNEL_ID=
# DISCORD_SOLIDITY_CHANNEL_ID=
# DISCORD_HR_CHANNEL_ID=
