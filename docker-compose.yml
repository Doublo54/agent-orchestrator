# ============================================================
# Discord Bot — Docker Compose
# ============================================================
# Runs OpenClaw as a simple Discord chat bot.
#
# Quick start:
#   cp .env.example .env        # fill in your tokens
#   docker compose up -d        # build + start
#   docker compose logs -f      # watch it boot
#
# Docs: https://docs.openclaw.ai/channels/discord
# ============================================================

version: '3.8'

services:
  openclaw:
    build:
      context: "https://github.com/openclaw/openclaw.git#v2026.2.6"
      dockerfile: Dockerfile
    container_name: openclaw-gateway
    restart: unless-stopped
    init: true
    user: root

    ports:
      - "127.0.0.1:${OPENCLAW_PORT:-18789}:18789"

    environment:
      - HOME=/home/node
      - TERM=xterm-256color
      - NODE_ENV=production
      - TZ=${TZ:-UTC}
      # Gateway
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN:-}
      - OPENCLAW_GATEWAY_BIND=${OPENCLAW_GATEWAY_BIND:-lan}
      - OPENCLAW_GATEWAY_PORT=18789
      - OPENCLAW_DISABLE_BONJOUR=1
      # LLM providers
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      # Discord
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN:-}
      # GitHub
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
      # PATH
      - PATH=/home/node/.local/bin:/home/node/.npm-global/bin:/usr/local/bin:/usr/bin:/bin

    volumes:
      - openclaw_config:/home/node/.openclaw
      - ./state/openclaw.json:/mnt/openclaw.json:ro
      - ./workspace:/home/node/.openclaw/workspace

    command:
      - sh
      - "-c"
      - |
        # Create required directories
        mkdir -p ~/.local/bin ~/.local/share ~/.npm-global \
                 ~/.openclaw/agents/main/sessions \
                 ~/.openclaw/credentials
        chmod 700 ~/.openclaw

        # Seed config (bind-mounting a file into a named volume
        # causes Docker to create a directory — copy instead)
        rm -rf ~/.openclaw/openclaw.json 2>/dev/null
        cp /mnt/openclaw.json ~/.openclaw/openclaw.json

        # Create openclaw CLI alias
        echo '#!/bin/sh' > ~/.local/bin/openclaw
        echo 'exec node /app/dist/index.js "$@"' >> ~/.local/bin/openclaw
        chmod +x ~/.local/bin/openclaw

        openclaw doctor --fix
        sleep 1

        # Start gateway (replaces this shell)
        exec node dist/index.js gateway --bind lan --port 18789 --allow-unconfigured

    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:18789/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s

volumes:
  openclaw_config:
    name: openclaw_config
